#include <stdio.h>

/**
 * main - Prints the sum of all multiple of 3 or 5 upto 1021
 * Return: Always (Success)
 */

int main(void)
{
	int g, w = 0;

	while (g < 1024)
	{
	if ((g % 3 == 0) || (g % 5 == 0))
	{
	w += i;
	}
	g++;
	}
	printf("%d\n", w);
	return (0);
}
