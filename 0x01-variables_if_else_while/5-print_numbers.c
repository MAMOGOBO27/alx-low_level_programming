#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * Return: Always 0 ( success)
 */

int main(void)
{
	int k;
        for (k =  0; k < 10; k++)
	printf("%d", k);
	printf("\n");

	return (0);
}
