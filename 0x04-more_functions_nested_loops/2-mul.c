#include "main.h"

/**
 * mul - Multiplies two integers
 * @b: The first number to multiply
 * @c: The second number to multiply with first number
 * Return: The result of multiplication  between x param and y param
 */

int mul(int b, int c)
{
	return(b * c);
}
